import CurrentUser from './CurrentUser';

class Child extends CurrentUser {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  setGuardian(email: string, phoneNumber: string) {
    // TODO: implement api call for setting guardian
  }
}

export default Child;
